// Importações
import React from 'react'
import { SectionContainer } from './styles'

// Componente Section como uma função que retorna JSX
export const Section: React.FC = () => {
  return <SectionContainer />
}
